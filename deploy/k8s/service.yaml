apiVersion: v1
kind: Service
metadata:
  name: prism
  labels:
    app.kubernetes.io/name: prism
    app.kubernetes.io/part-of: llm-d-benchmark
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prism
  ports:
    - name: http
      port: 8080
      targetPort: http
